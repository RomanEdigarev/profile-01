import React from 'react';

const ProfileMenu = () => {
    return (
        <div className={'app__content__profile-menu'}>
            ProfileMenu
        </div>
    );
};

export default ProfileMenu;